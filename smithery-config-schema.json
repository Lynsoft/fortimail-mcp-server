{
  "type": "object",
  "properties": {
    "mcp_bearer_token": {
      "type": "string",
      "title": "MCP Bearer Token",
      "description": "Must exactly match MCP_HTTP_BEARER_TOKEN on the server (no extra spaces; paste once).",
      "x-from": {
        "header": "x-mcp-bearer-token"
      },
      "x-to": {
        "header": "Authorization",
        "prefix": "Bearer "
      }
    },
    "verifyCert": {
      "type": "boolean",
      "title": "Verify Engine TLS",
      "description": "Whether the server should verify the FortiMail Engine TLS certificate. Disable for self-signed certs.",
      "default": true
    },
    "cacheBackend": {
      "type": "string",
      "title": "Cache Backend",
      "description": "MCP response cache backend.",
      "enum": [
        "memory",
        "redis"
      ],
      "default": "memory"
    }
  }
}